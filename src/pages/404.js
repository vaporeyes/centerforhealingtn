import React from 'react'
import Layout from '../components/layout'

const NotFoundPage = () => (
  <Layout>
    <h1>Page Not Found</h1>
    <p>You've found a page that doesn't render to anything!</p>
  </Layout>
)

export default NotFoundPage
